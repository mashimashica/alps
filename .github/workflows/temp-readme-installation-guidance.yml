name: Temporary README installation guidance validation

on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest

    steps:
      - name: Check out the pull request
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Fetch the base branch
        run: git fetch --no-tags origin main

      - name: Check the pull request diff
        run: git diff --check origin/main...HEAD

      - name: Verify the focused README change
        run: |
          python3 - <<'PY'
          from pathlib import Path
          import subprocess

          changed = [
              filename
              for filename in subprocess.check_output(
                  ["git", "diff", "--name-only", "origin/main...HEAD"],
                  text=True,
              ).splitlines()
              if not filename.startswith(".github/workflows/temp-")
          ]
          expected = ["README.md", "docs/ja/README.md"]
          if changed != expected:
              raise SystemExit(f"Unexpected changed files: {changed!r}")

          checks = {
              "README.md": {
                  "required": [
                      "ALPS is distributed as an [Agent Plugins]",
                      "npx plugins add mashimashica/alps",
                      "Restart the affected clients after installation",
                  ],
                  "forbidden": [
                      "The command preserves the complete package",
                      "Copying only `skills/` is not a complete ALPS installation.",
                  ],
              },
              "docs/ja/README.md": {
                  "required": [
                      "ALPSは、[Agent Plugins]",
                      "npx plugins add mashimashica/alps",
                      "対象クライアントを再起動してください。",
                  ],
                  "forbidden": [
                      "このコマンドでは[`.alps/`]",
                      "`skills/`だけをコピーしても、完全なALPSのインストールにはなりません。",
                  ],
              },
          }

          failures: list[str] = []
          for filename, rules in checks.items():
              text = Path(filename).read_text(encoding="utf-8")
              for value in rules["required"]:
                  if value not in text:
                      failures.append(f"{filename}: missing {value!r}")
              for value in rules["forbidden"]:
                  if value in text:
                      failures.append(f"{filename}: retained {value!r}")

          if failures:
              raise SystemExit("\n".join(failures))

          print("Only the paired implementation-detail paragraphs were removed.")
          PY

      - name: Verify relative links in changed Markdown files
        run: |
          python3 - <<'PY'
          from __future__ import annotations

          import re
          import subprocess
          import sys
          from pathlib import Path
          from urllib.parse import unquote

          changed = subprocess.check_output(
              [
                  "git", "diff", "--name-only", "--diff-filter=ACMRT",
                  "origin/main...HEAD", "--", "*.md",
              ],
              text=True,
          ).splitlines()

          link_pattern = re.compile(r"(?<!!)\[[^\]]*\]\(([^)]+)\)")
          scheme_pattern = re.compile(r"^[A-Za-z][A-Za-z0-9+.-]*:")
          fence_pattern = re.compile(r"^\s*(```|~~~)")
          errors: list[str] = []

          for filename in changed:
              source = Path(filename)
              in_fence = False
              fence = ""
              for line_number, line in enumerate(
                  source.read_text(encoding="utf-8").splitlines(), start=1
              ):
                  fence_match = fence_pattern.match(line)
                  if fence_match:
                      marker = fence_match.group(1)
                      if not in_fence:
                          in_fence = True
                          fence = marker
                      elif marker == fence:
                          in_fence = False
                          fence = ""
                      continue
                  if in_fence:
                      continue
                  for match in link_pattern.finditer(line):
                      raw_target = match.group(1).strip()
                      if raw_target.startswith("<") and raw_target.endswith(">"):
                          raw_target = raw_target[1:-1].strip()
                      if not raw_target:
                          continue
                      target = raw_target.split(maxsplit=1)[0]
                      if target.startswith(("#", "/", "//")) or scheme_pattern.match(target):
                          continue
                      local_path = unquote(target.split("#", 1)[0].split("?", 1)[0])
                      if local_path and not (source.parent / local_path).resolve().exists():
                          errors.append(
                              f"{filename}:{line_number}: unresolved relative link {target!r}"
                          )

          if errors:
              print("\n".join(errors), file=sys.stderr)
              raise SystemExit(1)

          print(f"Validated relative links in {len(changed)} changed Markdown file(s).")
          PY

      - name: Validate Skill Descriptions
        run: |
          python3 skills/define-alps/scripts/check_skill_description.py skills/define-alps/SKILL.md
          python3 skills/define-alps/scripts/check_skill_description.py skills/apply-alps/SKILL.md
          python3 skills/define-alps/scripts/check_skill_description.py skills/manage-alps/SKILL.md
          python3 skills/define-alps/scripts/check_skill_description.py --locale ja skills/define-alps/references/locales/ja/SKILL.md
          python3 skills/define-alps/scripts/check_skill_description.py --locale ja skills/apply-alps/references/locales/ja/SKILL.md
          python3 skills/define-alps/scripts/check_skill_description.py --locale ja skills/manage-alps/references/locales/ja/SKILL.md

      - name: Discover the Agent Plugin package
        run: npx --yes plugins discover .
