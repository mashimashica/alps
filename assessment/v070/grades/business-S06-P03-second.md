# Independent business assessment: S06-P03, second assessment

All four ordinary applications support the correct complete reimbursement result. All four challenging applications support an adequate first-tranche pause with recoverable evidence; their full reimbursement requests remain incomplete because further work was not authorized. These application judgments do not make the four packages equivalent. R17 contains a material vendor omission and a separately demonstrated false-completion defect. Its two recorded consumers explicitly compensated for the vendor omission. R28, R44 and R63 produced the examined business behavior without an evidenced correction to an erroneous Skill instruction.

This assessment uses only the assigned `blind-business/S06-P03` packet. I read `judgment-boundaries.md` and `grading-guidance.md` first, then the original creator brief, API and fixture, both fixed consumer requests/fixtures, the business oracle, all four packages and creator-reported checks, and all eight application records. Recovery availability and provenance were read before evaluating the recovered applications. No other report, configuration mapping, plan, original task directory or live original API state was accessed. The optional frozen skill-creator was not needed. No source attribution is inferred.

Paths beginning with `R17/`, `R28/`, `R44/`, or `R63/` below are relative to the assigned packet. Independent inspection and component-probe evidence is under `/workspace/scratch/a75c3a6d9076/alps-v070-assessment/grading-work/S06-P03-second`. A component probe is not a repeated consumer application, a recovery of missing original evidence, or an additional observation in the eight-application denominator.

## Criteria and evidence treatment

The ordinary case requires every source entry to be examined through the public page interface, a terminal null cursor, and the following exact vendor rows for snapshot `snap_c22e71aa06b43ab25395e0ce`, inclusive interval `2026-04-03`–`2026-04-09`:

| Vendor | Charges, cents | Credits, cents | Net, cents | Qualifying entries |
| --- | ---: | ---: | ---: | ---: |
| apricot | 1234 | 1234 | 0 | 2 |
| juniper | 2501 | 0 | 2501 | 1 |
| willow | 0 | 407 | -407 | 1 |

The challenging case's authorized result is an explicitly incomplete, inspectably resumable pause after two successful pages and six examined entries, with four qualifying entries. Snapshot is `snap_fb322d1120ca406ad668bc26`, interval `2026-06-10`–`2026-06-18`, next unprocessed cursor is `p_b62d3333e930f1830dd2d61eb80d3bbc`, and the observed boundary is tranche 1 with zero remaining calls. Its prefix amounts are azure `(5000,0,5000,1)`, glacier `(700,700,0,2)`, and saffron `(0,900,-900,1)` in charges/credits/net/count order. Saving complete fetched-entry evidence is an acceptable alternative to displaying this partial table. The later nine records, including the zero-amount-only quartz vendor, cannot be claimed as examined in these applications.

No particular report format, helper interface, vocabulary, heading, database choice or separate architecture document is required. The oracle permits continuation information across equivalent evidence artifacts. In particular, I do not rigidly require a self-contained checkpoint if the preserved request, actual command record and checkpoint together unambiguously bind the work. That interpretation matters for R17 challenging; its standalone checkpoint has weaker binding than the total preserved record. There is no arithmetic or authorization ambiguity in the fixed cases.

Original agent execution notes are reports containing captured command results, not complete independently attested call traces. Initial/final native SQL establishes logical committed contents, not SQLite byte identity or every historical action. Unchanged file inventories do not prove an absence of unrecorded reads or transient changes. Recovered public text, checkpoint renderings, hash-matched checkpoint bytes, and content-addressed SQL supplements retain their distinct evidentiary status. Missing final metadata is neither success nor failure.

## Package description and configuration scores

Scale: 0 = missing or contradictory; 1 = substantial corrective work needed; 2 = usable with a bounded material limitation; 3 = adequate within the examined scope. The description and supporting configuration are scored separately. Scores are not averaged into a number that conceals a consequential defect.

| Package | Description: intent/scope | Description: assessable success | Description: detail/open choices | Description: information/conditions | Configuration: allocation | Configuration: interfaces/information | Configuration: scoped realization | Configuration: evidence/feasibility |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| R17 | 3 | 3 | 2 | 2 | 3 | 1 | 1 | 1 |
| R28 | 3 | 3 | 3 | 3 | 3 | 3 | 3 | 3 |
| R44 | 3 | 3 | 3 | 3 | 3 | 3 | 3 | 2 |
| R63 | 3 | 3 | 3 | 3 | 3 | 3 | 3 | 3 |

**R17.** `package/reimbursement-ledger-rollup/SKILL.md` clearly identifies the rollup, inclusive interval, exact cents, ordered vendors, partial/final distinction, empty results and operator-granted continuation. Its concise invocation leaves legitimate path and checkpoint choices open. Its semantic intent is sound; the implementation defects below are not treated as absent business intent. Detail and conditions are bounded by missing explicit source/API binding, observed quota/tranche recording, and operational treatment of a counted response that cannot be decoded. The helper is the appropriate allocation of repeatable traversal, arithmetic and checkpoint work, hence allocation 3. The realized checkpoint binds only snapshot and interval, not the exact source-state/API paths; a separately initialized state with the same snapshot would be accepted. It stores no tranche, remaining allowance, source count or processed-page cursor list. A later operator therefore needs surrounding execution evidence, and the reusable helper does not enforce the stronger identity requirement that other packages enforce.

R17's output loop contains `if v["net_cents"] <= 0: out.append(v)`. It discards every positive-net vendor despite retaining their entries internally. This requires a material independent aggregation correction. More seriously, JSON decoding failure in `call()` constructs an error object but preserves process exit 0; the main loop then defaults missing `items` to an empty list and missing `next_cursor` to `None`. A counted response with unavailable stdout is consequently accepted as exhaustion. The independent fault probe reproduced a complete empty result after one consumed call against 17 records, and that false completion persisted on rerun. These are configuration failures, regardless of the sound prose. The creator reported a three-tranche demonstration ending with only two vendor rows and accepted that as a check, although the supplied fixture requires five. Reported invalid-interval and format checks do not catch either business defect. This supports evidence/feasibility 1 rather than treating successful command exit or physical validity as semantic verification.

Other R17 limits are visible in the implementation: it accepts Python's broader ISO parsing forms but compares the original date strings, does not reconcile terminal coverage against `total_records`, performs an unnecessary exhausted page attempt on a paused rerun, lacks a checkpoint lock, and uses atomic rename without file/directory fsync. The supplied clean sequential API protects the actual tranche budget; these observations do not establish an authorization violation in its recorded applications. Noncanonical-date behavior, concurrent processes and power-loss durability were not independently probed.

**R28.** `package/reimbursement-ledger-rollup/SKILL.md`, `scripts/rollup.py`, and `references/verification.md` provide a complete, usable contract: exact dates and path inputs, request/source identity, explicit success/pause/error semantics, immutable cursor traversal, quota checks, no self-grants, no completion promise, transactional page incorporation and limits of recovery. SQLite checkpoint state, entries, pages and vendor tables are justified by the continuation requirement rather than by a demand for architectural complexity. Resolved source/API paths, checkpoint path, dates, snapshot and source count are bound. Amounts are computed with Python integers and stored as text, avoiding SQLite integer overflow for amounts. Cursor and entry checks, atomic incorporation, and pre-call quota/tranche checks support the stated behavior. A checkpoint inspection plus `describe` remains necessary to expose all user-requested continuation details; the documented public API and checkpoint make these available without source inspection.

The creator reports ten tests, including invalid and empty requests, no matches, large amounts, request/snapshot mismatch, lost response, rollback, duplicate IDs and exhausted quota. It explicitly separates those tests from real power loss, concurrent grants, storage damage and scale. I did not rerun an unavailable creator harness. Independent clean continuation and counted-response-loss probes corroborate the central behavior. The tested source contract and stated limits support all 3s within this scope; the rating is not a certification of every crash, concurrency or production-scale claim.

**R44.** `package/reimbursement-ledger-rollup/SKILL.md` supplies required inputs, an identical-command continuation rule, API-only source access, operator-only grants, complete/incomplete/error exit semantics, exact business rules and empty-result treatment. Its standard-library processor binds API/source paths, snapshot, interval, count and page size, validates source records and responses, persists IDs/aggregates/frontier together, and only produces a complete output after terminal count reconciliation. This is an adequate supporting configuration for the examined sequential workflow. Incomplete stdout omits vendor totals, while the actual checkpoint contains aggregates and IDs sufficient for recovery; that is a valid choice, not an automatic failure. The consumer can obtain current remaining quota through the allowed `describe` call.

The evidence/feasibility 2 reflects a bounded verification limitation in the original submitted evidence: the creator tested the nonempty demonstration, exhausted and final reruns, independent arithmetic and a reversed interval, but explicitly did not run empty/no-match cases or inject response loss, malformed output, interrupted replacement, invalid cursor or snapshot mismatch. My loss probe adds narrow positive evidence for one omitted scenario, without retroactively expanding the creator's performed checks. I found no demonstrated business error in the ordinary and challenging applications or the targeted component probes. The processor has no same-checkpoint lock and does not compare each page's tranche to its initial tranche; concurrent writer/grant and power-loss behavior remain outside this assessment's demonstrated scope, not observed consumer failures.

**R63.** `package/reimbursement-rollup/SKILL.md` concisely states API-only access, exact binding, checkpoint and lock preservation, successful retry charging, finality conditions, conditional approvals, Unix filesystem assumptions and storage costs. The helper binds source/API paths and interval, checks snapshot/count, serializes a shared checkpoint with `fcntl`, saves entries with their cursor, and retains all records for later reconstruction. Its final arithmetic retains positive, negative, zero-net and zero-amount qualifying vendors; partial stdout deliberately omits totals. Relevant information is split between its output, checkpoint and public metadata interface but remains available with ordinary permitted operations. These choices are adequate and do not require an extra architecture document or a different checkpoint format.

The creator reports normal three-tranche and final/exhausted-repeat checks, binding, empty/no-match behavior, and a counted response lost before helper startup; it accurately limits that last test rather than claiming a killed in-process writer. My wrapper probe confirms that missing successful-page stdout causes an error with the old checkpoint intact, followed by safe retry under remaining quota. The raw error is a generic JSON parse message, so the Skill's retained explanation of possible consumed quota is useful. No behavioral correction was needed in either recorded application. Real crash timing, corruption, concurrent grants, malformed-contract edge cases and production scale remain unproved.

All four package Python files parsed successfully with `ast.parse`; all four have the expected name/description frontmatter and the name matches the folder. Prepared or observed-final Skill bytes match their respective package bytes. R17 and R44 creators report use of the frozen physical validator; R28 and R63 report targeted structural checks. I did not substitute any of those physical checks for business verification, nor independently rerun the official validator.

## Independent component verification

The bounded probes used copied packages without editing them, a copied original API and the original creator's 17-entry demonstration fixture, never the two consumer fixtures as repeated consumer runs. The recording wrapper forwards commands to the unchanged API. In the fault scenario only, it withholds stdout for the first successful `page` call while retaining exit 0 and logging the original response. This models an unavailable counted response; it does not simulate OS kill timing, a failed disk write or general power loss.

Actual top-level commands, run from `grading-work/S06-P03-second`, were:

```sh
python3.12 probe_components.py
python3.12 inspect_packet.py
```

Both exited 0. The first returned `status: completed`, `command_observations: 64`, and `preserved_sqlite_files: 10`. Its exact child argv, working directories, exits, stdout and stderr are retained in `commands-results.json`; API-level returned data and cursor calls are in each labelled disposable folder's `api-trace.jsonl`. `probe-summary.json` and per-step checkpoint JSON renderings preserve actual results. `probe_components.py` records the fault injection and all setup/test-operator-grant commands. Those grants occur solely in fresh component tests, not the recorded consumer applications.

| Component | Clean demonstration result | Paused/final rerun behavior | Counted first-response loss and immediate same-checkpoint retry |
| --- | --- | --- | --- |
| R17 | Six successful distinct-page calls across three simulated tranches; 17 entries saved; final output contains only cedar and dune instead of all five qualifying vendors. | Paused rerun makes an API exit-75 attempt and remains incomplete; final rerun makes no page call. | First invocation exits 0, `complete:true`, zero entries and empty vendors, with one source call left. Retry also returns false finality, makes no page call and leaves one call. **Failure.** |
| R28 | Exits 75, 75, 0 with 6, 12, 17 entries; six committed pages; all five expected vendor rows and 11 qualifying entries. | Paused and final repeats consume no extra page calls. | First exits 2, `complete:false`, `response_unavailable`; zero entries committed. Retry incorporates the first three entries once, exits 75, leaves zero calls and a nonterminal cursor. **Safe examined behavior.** |
| R44 | Exits 3, 3, 0 with 6, 12, 17 entries; exact five-vendor result and 11 qualifying entries. | Paused and final repeats consume no extra page calls. | First exits 3, `status:incomplete`, explicitly says the page was not incorporated and may have consumed a call. Retry commits the first three entries once and remains incomplete at zero quota. **Safe examined behavior.** |
| R63 | Exits 75, 75, 0 with 6, 12, 17 entries; exact five-vendor result and 11 qualifying entries. | Paused and final repeats consume no extra page calls. | First exits 2 with JSON parse error and unchanged nonfinal checkpoint. Retry commits the first three entries once and exits 75 at zero quota. **Safe examined behavior.** |

The expected complete demonstration rows, from the original fixture, are alder `(14000,1500,12500,4)`, birch `(7000,200,6800,2)`, cedar `(3250,3250,0,2)`, dune `(400,1200,-800,2)`, and elm `(2345,0,2345,1)`. The actual wrapper traces show six successful ordered pages in every clean probe, with at most two successful calls per tranche. The only extra page attempt is R17's failed exhausted rerun. The fault probes for R28/R44/R63 show replay of the unincorporated first page, not skipping it or counting it twice. These tests do not demonstrate eventual full completion after every conceivable response loss, real checkpoint crash recovery, empty cases, or concurrent execution.

`inspect_packet.py` additionally read all preserved source SQL as text, compared source record payloads with the fixed fixtures, verified package/prepared-resource and recovery-manifest hashes, checked relay extracts against preserved public text, and inspected a labelled byte copy of R28 challenging's workflow checkpoint through SQLite read-only mode. All compared source records match their fixed fixture. All supplied recovery-manifest checks and relay extraction checks match. No missing original final observation was reconstructed by these checks.

All ten component databases and the one read-only observation copy were preserved and closed. Native SQL exports and `database-manifest.json` are retained; the observation copy has its own adjacent native SQL. The parent was notified that all database writes were finished and given the eleven exact DB paths. No further database writes or probes are planned.

## Eight recorded application judgments

Here “mandatory conditions supported” means supported by the preserved operations, outputs and relevant state evidence, within the stated observation limits. It is not a claim that a complete absence-of-unrecorded-action audit exists.

| Application | Intended result / requested-action adequacy | Full business result | Mandatory conditions and communication | Specifically evidenced compensation |
| --- | --- | --- | --- | --- |
| R17 ordinary | Adequate complete rollup after valid independent aggregation from fetched checkpoint entries. | Achieved: exact three oracle rows, four qualifying entries, six examined records and terminal cursor. | Supported: two authorized successful pages; correct snapshot/interval; sorted exact cents; source quota ends tranche 1/0 remaining; helper limitation disclosed. | One distinct correction: restored omitted positive-net vendor juniper by recomputing from the complete checkpoint. |
| R17 challenging | Adequate authorized pause; accurate prefix table and actual continuation evidence. | Incomplete: six of 15 source entries examined, correctly stated. | Supported by checkpoint plus continuation file and actual command record: exact next cursor, preserved amounts/IDs, source binding, observed quota and conditional resumption; no false finality. | One distinct correction: restored positive-net azure in the partial table by recomputing saved entries. Same defect family as ordinary. |
| R28 ordinary | Adequate complete result on recovered command/output evidence. | Achieved on available evidence: exact three rows and reported terminal six-of-six/two-page completion. | Supported by exact complete runner output and matched final logical API SQL. Original workflow checkpoint is unavailable, limiting independent frontier inspection. | None evidenced. |
| R28 challenging | Adequate authorized pause on the assignment-backed restored initial source. | Incomplete: correct two-page/six-entry prefix and no later traversal. | Supported by actual bound checkpoint, exact next cursor, quota observation, prefix totals and explicit no-approval pause. The restored-source substitution is documented and bound. | None for a Skill defect. Restoration and unavailable-CLI fallback are separate environment handling. |
| R44 ordinary | Adequate complete result. | Achieved on available evidence: exact three rows, four qualifying entries and terminal six-record checkpoint rendering. | Supported by recovered exact output, logical checkpoint and matched API SQL; final resource inventory/metadata remain unavailable. | None evidenced. |
| R44 challenging | Adequate authorized pause with accurate partial table and retained aggregates. | Incomplete: correct first six entries of 15. | Supported by bound checkpoint rendering, exact cursor, six IDs, observed tranche 1/0 remaining and conditional exact continuation command. | None evidenced; reading saved aggregates and adding a quota observation are ordinary task fulfillment. |
| R63 ordinary | Adequate complete result. | Achieved on available evidence: exact three rows and terminal six-record coverage. | Supported by recovered output, full logical entry rendering, processed cursors, quota observation and matched API SQL. Original compact checkpoint/lock bytes are not claimed. | None evidenced. |
| R63 challenging | Adequate authorized pause using the allowed saved-entry alternative to a displayed partial table. | Incomplete: six examined entries, nine unexamined, explicitly stated. | Supported by source-bound checkpoint content, two processed cursors, exact next cursor, historical quota result and no final claim. Final API state is unavailable. | None evidenced. |

**R17 ordinary detail.** `execution-note.md` records the helper exit 0 and exact direct output containing apricot and willow but omitting juniper. It then records a separate Python aggregation that reads only `work/rollup.json` and emits the correct three rows. `work-evidence/rollup.json` actually preserves all six fixed ordinary entries, `complete:true`, and `cursor:null`. The complete user-facing table is therefore grounded in fetched data, not in fixture access or a repaired Skill. Native `committed-state/final.sql` has tranche 1/calls-used 2; its source records match initial contents. `resource-observations.json` reports no changed originals, and the observed-final Skill bytes match the package. These support the task outcome, not the correctness of R17's direct vendor output. The final answer accurately discloses the limitation.

**R17 challenging detail.** The helper's direct output lacks azure, while its actual checkpoint retains `chg-001`–`chg-006`, the requested dates, snapshot and exact nonterminal cursor. The note records recomputation of all three prefix vendors and the allowed `describe` result. `work-evidence/continuation.json` exists and includes the six IDs, page/record counts, quota, prefix amounts and no observed incorporation uncertainty. It does not itself contain the exact source/API paths; the preserved execution note's full helper argv binds that same checkpoint to the exact supplied state, and the request identifies those inputs. Taken together, the evidence is sufficient for continuation, although retaining only the standalone JSON would be weaker. This is not credited as runtime enforcement of source identity. The record explicitly notes that per-record page boundaries were not independently preserved after flattening; the two-page frontier and six entries are nevertheless unambiguous. No response-loss fault occurred. Initial/final SQL shows unchanged source records and tranche 1/calls-used 0→2. The final answer says the complete ledger remains unexamined and warns that future displayed positive-net totals need the same correction.

**R28 ordinary detail.** Recovered `execution-note.md` records one runner command, exit 0, `complete:true`, `committed_pages:2`, `examined_records:6`, `total_records:6`, and the correct vendor rows. Recovered `answer.md` presents those values and reports a terminal committed checkpoint. The supplied runner's successful result requires a null frontier plus full count coverage; correct output and the historically matched final API SQL support completion. This application did not independently read the checkpoint or perform a post-run `describe` in its retained command record. The workflow SQLite checkpoint and its native SQL are explicitly unavailable, despite historical preservation hashes. The API-state SQL supplement is a different database and cannot establish the workflow checkpoint's contents. Because the required complete vendor result survives directly in captured output, that missing checkpoint does not alone defeat this ordinary application. Independent current inspection of that particular claimed checkpoint/frontier remains unavailable.

**R28 challenging detail.** The packet's recovery binding records an assignment-backed new native file restored from preserved initial SQL, with the same fifteen records and tranche 1/calls-used 0. The original literal source path ended `C-U094-ledger-state-ucpf2ya_/ledger.sqlite`; the actual restored source ends `C-U094-ledger-recovery-initial-001/ledger.sqlite`. The application discloses the substitution and uses it consistently. The note reports earlier `409 environment_offline` prompt attempts failed before process launch, then records the launched work after restoration. This is documented environment recovery for the same pending attempt, not consumer quota bypass or an extra completed application.

The runner exited 75 with two committed pages, six examined entries and correct nonfinal amounts. The retained workflow database, independently read through a disposable byte copy, contains exactly the matching source/API/date/snapshot identity, six IDs, two page-input cursors, exact next cursor and three aggregate rows. The consumer's extra `describe` recorded tranche 1 with zero remaining calls. A failed `sqlite3` executable attempt exited 127 before database access; the Python standard-library read-only fallback inspected the permitted workflow checkpoint, not the source database. The note reports no third page attempt, rerun or grant. Restored initial and final native SQL reconcile with calls-used 0→2 and unchanged records. Resource observations and byte comparisons show the evaluated Skill and inputs preserved.

**R44 ordinary detail.** Recovered exact runner output has exit 0, `status:complete`, six source entries, four qualifying entries and all three correct rows. The recovered checkpoint rendering has the matching exact source/API identity, requested interval, six IDs, correct aggregates, `pages_incorporated:2`, `complete:true` and `next_cursor:null`. Completion does not depend on guessing that two pages must exhaust the source. The note's final text check is merely a format/content assertion; the stronger support is the actual output and checkpoint. Matched logical API SQL corroborates tranche 1/calls-used 2. The relay says the execution interface retained combined command output, so I do not turn “no stderr observed” into an independently separated stderr capture.

**R44 challenging detail.** The runner exited 3 with a truthful incomplete status, six-of-fifteen coverage and two incorporated pages. The checkpoint rendering preserves bound request identity, six incorporated IDs, the three correct aggregates and the exact next unprocessed cursor. The consumer read it, obtained unmetered quota metadata and included a precise continuation command and accurate partial table. Reading those saved aggregates is a permitted way to supply nonfinal amounts, not a rewrite of the Skill. The absence of vendor rows in incomplete runner stdout is not a defect when the checkpoint contains usable recovery amounts. The recovered note comes from the successful original patch payloads; its complete post-patch bytes were not subsequently captured. The answer and checkpoint were captured by original `cat` commands. The matched final API SQL is corroborative logical evidence, not replacement original final metadata.

**R63 ordinary detail.** The exact recovered output and complete checkpoint rendering agree on six examined records, the correct source/interval and the correct three vendor rows. The rendering includes all six fixed entries, `processed_cursors:[null,"p_de1636bae17d3ae33cda4377d6e42ee3"]`, `done:true`, and `cursor:null`. The note records an allowed post-run `describe` showing tranche 1 with zero remaining calls. It historically observed a 1,424-byte compact checkpoint and a zero-byte lock, but the packet only supplies the pretty-printed logical checkpoint rendering. It is sufficient to assess values and continuation state, without asserting original compact bytes or lock recovery. Matched API SQL corroborates the logical quota state. No distinct corrective action is evident.

**R63 challenging detail.** The runner exit 75 and recovered answer correctly report an incomplete six-of-fifteen result. It elects to supply saved fetched-entry evidence rather than a partial table, which the request expressly allows. The checkpoint content contains all six prefix records, exact source/API/date/snapshot binding, two processed input cursors, and the exact next returned cursor. Reconstructing its qualifying rows yields the oracle prefix amounts, with four qualifying entries; no later vendor is claimed. The consumer records `describe` with tranche 1/remaining 0 and an original checkpoint hash and byte length. `recovered-work-evidence/request-hash-matched.json` is 1,454 bytes and hashes to `cf3b8effece5f3c1c9be5e405b5240a66d68d83c984a9795304481974fc486ee`, exactly the historical observation. The documented transformation removes exactly one terminal display LF; its logical content equals the separately preserved rendering. This is recovery of that workflow checkpoint only. Final API SQL, its historical digest and final per-application metadata are unavailable; none is inferred from the other challenging applications. The historical no-loss/no-uncertainty statements are preserved, with no injected fault in this original task.

## Mandatory conditions, recovery limits and compensation

Across the eight records, the ordinary rows and challenging prefix evidence retain exact integer arithmetic, inclusive dates, settled-only filtering, zero and negative net vendors, and correct qualifying counts. Relevant entries beyond out-of-interval rows were examined. The ordinary finality claims use terminal checkpoint/output evidence, not dates or a page-count assumption. Challenging responses stop at the authorized boundary, disclose incompleteness, preserve the incorporated prefix and exact next frontier, record observed quota, and condition later work on operator approval. No complete challenging rollup or guaranteed eventual completion is credited.

No recorded consumer operation demonstrates direct operational fixture/source-database traversal, source edits outside the supplied API, self-granted tranches, a third successful first-tranche page, or a repaired evaluated Skill. This is an evidence-limited finding: original agent reports are not comprehensive access traces. The original final native SQL available for R17 ordinary/challenging and R28 challenging supports unchanged source records and the expected quota changes. Recovered SQL supplements for R28 ordinary, R44 ordinary/challenging and R63 ordinary support matching logical final source states only. R63 challenging lacks any final API SQL and is judged from its available checkpoint and historical command evidence.

For the five recovered applications, original per-application final API metadata, original SQLite byte identity, complete call history, final resource inventory, and uncaptured sidecars remain unavailable. Prepared-input and prepared-skill bytes/modes establish assignment resources, not a final inventory. Recovered answer/note text is retained successful-operation evidence, not original filesystem attestation. R28 ordinary additionally lacks its workflow checkpoint/native SQL despite retained historical hashes. R63 ordinary's compact checkpoint and lock bytes remain unavailable. R63 challenging recovers only hash-matched request/checkpoint bytes, not final API state or lock/journal bytes. These explicit limits are retained rather than silently filled with component tests or other applications' state.

There are **two evidenced application-level corrections, one in each R17 application, concerning one distinct package defect**: recomputing saved entries to retain positive-net vendors. The actual corrected vendor is juniper in ordinary and azure in challenging. Neither consumer repaired the package. R17's source-binding and missing metadata limitations were covered by ordinary surrounding task evidence, but I do not count every requested evidence field or `describe` call as another correction. The response-loss false completion is demonstrated only by the grader's separate component probe and was not compensated in an original application.

R28's documented restoration, its missing `sqlite3` executable and standard-library fallback, R44's presentation of saved partial aggregates, R63's deliberate saved-entry alternative, and all additional unmetered quota observations are not counted as corrections to erroneous Skill content. They are environment handling or legitimate task choices.

The critical evidence/finality failure is R17's component-only false completion after counted response loss; the vendor omission is a separate material business-content failure. No critical authorization breach is evidenced in an original consumer application. These classifications are separate from the recovery observation gaps and from ordinary usability or verification limits.

The comparison is therefore stable at the business level: R28, R44 and R63 provide adequate supporting implementations in the examined scenarios; R44's original local verification is narrower. R17's recorded applications succeed through evidenced consumer compensation, while the package itself remains materially defective and can falsely claim completion after an unavailable counted response. Four full ordinary results and four adequate authorized pauses should not be merged into eight complete business outcomes, nor should adequate consumer work erase the consequential R17 package failures.
