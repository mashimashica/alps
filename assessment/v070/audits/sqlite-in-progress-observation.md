# Committed state observed while creators remain active

Before the next checkpoint, control-044 and control-045 still have running notifications. The UTF-8 exporter stopped on control-044's unindexed verification SQLite before any Git mutation. Root then used the already reviewed, read-only native-SQL helper on six explicitly identified SQLite-header paths in these two trial directories, under observation label in-progress-observation-001. This observes committed state; it does not mark either creator or its verification complete, replay processing, alter the source or influence its task.

The original databases remain mutable while their creators work. Any subsequent byte change must be preserved under a new distinct observation before checkpoint export will accept that source. On completion, reconcile the explicit indexed hashes with actual final sources and preserve new observations as needed. Earlier SQL remains evidence even when the current index advances. The helper checks a consistent read transaction and unchanged source bytes during capture; final checkpoint export verifies the snapshot's actual source hash. A refusal remains a preservation issue, not a business failure or a reason to rerun a creator.

After both C044 and C045 reported completion, root checked all six actual source hashes against their indexed observations. All six were unchanged. Their saved logical states therefore also represent those completed source endpoints; the original observation label remains unchanged. No additional replay or fabricated operation history is inferred. C046's four completed verification databases were separately captured under creator-complete before preparation of its consumers.
