# Explicit native SQLite evidence preservation

The next checkpoint attempt stopped during UTF-8 export at trials/control-037/verify1/source.sqlite. It had fetched the current evidence ref/commit but had not created a Git tree or commit or changed the branch. The last confirmed checkpoint remained cdbb51b991c9d6efe44dd61a602f8e41fcfbb4ce. The creator subsequently reported completion; all three verification databases remain in its original folder.

preserve_sqlite_evidence.py takes explicitly named regular SQLite files. It uses a consistent read-only transaction, verifies the database, checks source bytes did not change during capture, and writes native SQL plus hash metadata under a distinct observation label. It does not rerun the creator's processor, consume source API quota, reset a state or alter the original database. It refuses an active journal requiring separate preservation. sqlite-evidence-index.json identifies only those explicitly represented binary files. checkpoint.py verifies their present source hashes and saved SQL/metadata hashes before exporting the text representations in place of unsupported database bytes.

Root preserved the three completed control-037 verification states and restored each native SQL into a separate in-memory database. Its complete logical dump matched the original, and source hashes remained unchanged. audits/sqlite-evidence-preservation-check.json records those three passing checks. This establishes recoverable committed logical state only; the original database bytes, runtime identity and operation history are not claimed saved. A later source change invalidates the current representation and requires another explicit observation; missing SQL or mismatched hashes stops the checkpoint.

The first blind-packaging review's original helper and bounded probe output are also retained under component-evidence/control-packaging-first-review/. They document the original findings; the helper there is experiment evidence and is not used for live preparation. Independent follow-up review of the corrected blind helper and this narrow checkpoint integration is pending at this update. No C consumer has yet run, and no quality or release claim follows from these preservation checks.
