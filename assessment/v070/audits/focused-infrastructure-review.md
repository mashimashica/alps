# Focused infrastructure review — 2026-09-08

The checked infrastructure is usable for the fixed comparison. Creator staging passed before any A work began. Consumer staging is usable after its recorded completion and format-observation prerequisites. Packet staging is usable after all four assigned creators and eight applications in a packet have the required completion evidence. No blocking evidence-validity defect was observed in this bounded review.

This is infrastructure bookkeeping, not a creator/application trial, business grade, fidelity review, or release decision. No evaluated artifact, original state, implementation, or product file was edited. The only native API calls were `init` and `describe` on one disposable copied fixture/API. No new test framework was built.

| Boundary | Verdict | Observed scope |
| --- | --- | --- |
| A creator staging | Usable; early verdict sent to coordinator | All 12 actual prepared folders, fixed pairings, assignments, raw inputs and prompts checked before launch. |
| F consumer staging | Usable after explicit creator completion | Completion refusal, source/prompt/copy contracts inspected; one disposable S06 setup/snapshot path exercised. Actual F packages were not staged by this review. |
| Focused packet staging | Usable after complete assigned evidence | All 12 selected C source plans and 24 applications checked; recovered evidence roles retained; one disposable low-level writer check. A packets could not yet complete preflight. |

## Reviewed identities

- `prepare_focused.py`: `c7765c2664a1f1fadf13dbc4f5ccfbd0d145ec008eabd1b48f3943168eafabaf`.
- Final `prepare_focused_grading.py`: `37c0d7c2f4771fee029e49bcfeeaeb19438a26f0aab801e1daf50e8a472eca9c`.
- `focused-grading-allocation.json`: `5e0e5ef690576cad8afaa4d35d631a51761734f8d6778730f0ddda3aafc1601b`.
- Normalized original C creator prompt: `5842bdcfd6d0b07fea93987b406c765f0ab31910742f2b046fbd2e87b355fa4d`.

The C source-plan run used packet-helper version `40959c97156a25eafae1fc9466e95d2f08e0260536c076ee9d5547cba3e85c1f`. The subsequent final change replaces duplicated common-aid files with permission to read the existing frozen aid, retaining its externally observed hashes/modes. That delta was inspected; the final helper was used for the disposable writer and incomplete-packet checks. The underlying C source-role routines did not change in that delta.

## Creator inputs and allocation

A read-only Python check compared each actual A prompt directly with its paired C prompt after removing exactly one assigned ALPS sentence and reversing only the task-directory substitution. All twelve comparisons were exact. The S03 addition names description guidance, S06 names work-system guidance, and S10 names both without introducing a design sequence or task-specific advice. The common aid and physical-format orientation are unchanged in the paired prompt body.

Every copied creator input file matched both its original C counterpart and the frozen public manifest. The complete input file sets contained 1 file for S03, 3 for S06, and 2 for S10, with matching source modes. All actual focused assignments matched the fixed family/model/effort/repetition/pair metadata and prompt/input identities. The source identity check accepted all 70 frozen ALPS file/link entries and all 9 frozen common-aid files.

At this check, all 12 A creator ledgers said `not started`, all deliverable folders were empty apart from the prepared `skills/` directory, all 24 F consumer slots said `not prepared`, and no real F consumer folder existed. The copied public S06 creator fixture was the original supplied creator material. No consumer case, hidden consumer fixture, oracle, grade, or extra task instruction had been added to a creator folder. The helper's creator preparation branch does not read or materialize the consumer bank.

The fixed schedules contain exactly 12 creator cells and 24 consumer slots. The six packet allocations contain all 24 intended A/C generated Skills once, two A and two C per fixed family/creator-setting cell, with the prescribed ordinary/challenging applications. The label permutation is pinned independently of the new A outputs. Both actual C schedules and mutable assignment fields also passed their original fixed-identity checks.

## Completion and native-state checks

`completed(['focus-005'])` refused with `Coordinator-confirmed creator completion required: focus-005`. `final_snapshots(['F-U009'])` refused with `Coordinator-confirmed focused S06 consumer completion required: F-U009`. These calls only read actual coordinator evidence. The preparation code additionally requires nonempty creator execution notes and public final responses, exact input/prompt identities, the single supplied Skill, and the matching preserved physical-format observation. A nonzero format result remains evidence; it does not remove an assigned consumer or authorize repair.

The disposable probe lives in `audits/focused-infra-work/`. It imported an exact copied helper from its own assessment directory, with exact copied ordinary S06 fixture and public API. The copied setup's `F-U009` identifier is only a fixture for the adapter's closed identifier checks; it is not the actual scheduled application.

Both captured API calls returned exit 0 and empty stderr. Setup produced snapshot `snap_c22e71aa06b43ab25395e0ce`, 6 records, tranche 1, two remaining calls, and no successful page calls. Read-only SQL inspection found `calls_used = 0`, the exact fixture records in source order, and `PRAGMA quick_check = ok`. Initial and final native SQL were identical, with SHA-256 `1206581aaa26d95a4831a91f82dd1b59f06cfc56195294559db2af8192f63204`. Loading that SQL into memory reproduced the same logical state. Source/copy bytes and modes stayed unchanged, and the native database's byte hash did not change during snapshot checks.

Repeated setup, a snapshot under another consumer identifier, and a repeated final snapshot were refused without replacing evidence. Full exact calls/results, source identities, and rejection messages are preserved in `audits/focused-infra-work/native-probe-observation.json` and `audits/focused-infra-work/assessment/consumer-setup/F-U009.json`.

The native file remains at `audits/focused-infra-work/F-U009-ledger-state-cva3myd1/ledger.sqlite`. The coordinator was asked to preserve it and registered its read-only SQL/metadata under `sqlite-evidence/focused-infra-adapter/`; the matching entry is present in `sqlite-evidence-index.json`. No database was encoded or deleted. These SQL observations establish committed logical state, not a complete operation history or durable SQLite byte identity.

## Packet identity and recovery handling

The read-only source-plan run accepted every selected C creator and both applications. It checked original creator assignment/input/prompt identities, source/frozen package hashes and modes, raw-case substitutions, public interfaces, recorded copied-resource hashes/modes, the original application prompt, completion fields, and the label scan.

For the eight reused S03/S06 candidates, the previous mapping and packet-provenance hashes were pinned; every candidate file/mode and its surviving original source role or reviewed recovery derivation matched exactly. The selected recovery applications C-U085/086/087/088/093 retained their separate prepared-resource, recovered-text, logical-state-supplement and availability roles. C-U094/095/096 retained their original setup chain, restored-state binding and final native observations. C071/072 creator recovery plans passed with the canonical package, retained note, authored-text relay and physical-format observations kept distinct from lost original observations. No missing historical final state or workflow checkpoint was regenerated.

Exact per-candidate outcomes and counts are in `audits/focused-infra-work/packet-c-source-observation.json`. The selected reused candidates total 227 copied files; the four S10 C candidate plans add 100 copied files and 10 generated observation/provenance files. These are planned file counts, not new packets or trial counts.

The final helper's low-level writer was exercised once with two disposable text source files and one generated scope file. Both source bytes and modes (0644 and 0444) survived copying, the complete destination inventory matched the plan, and external metadata read back correctly. Repeating the write and supplying a separate partial destination both refused while preserving the existing files. The writer prints its fixed production count message even in this deliberately minimal fixture; that message is not evidence that four Skills/eight applications were packaged here. Scope and actual file counts are recorded in `audits/focused-infra-work/packet-writer-observation.json`.

Actual `build_plan('FP01')` stopped at the expected incomplete `focus-002` completion boundary and created no actual packet. Static review confirmed selected completion rows and source inventories are rechecked after copying, explicit arm/model/repetition metadata and other grades are excluded, and complete or partial destinations are never overwritten. Raw paths and substantive style can still reveal condition hints, as the fixed protocol states; this is incomplete procedural blinding.

## Remaining scope limits

No real F consumer staging or complete new four-package/eight-application packet was executed by this review; those require evidence from tasks that had not completed. Each actual packet must pass its own `--check-only` preflight and copy readback after completion. The helper refuses incomplete required evidence and offers no substitute trial or fallback package. Any nonrecoverable missing slot therefore needs an explicit coordinator disposition while retaining its fixed denominator.

The final helper correctly permits the single existing common aid instead of copying its display assets into every packet. Native SQLite files already present as application work evidence are still copied as substantive evidence. In particular, the planned `blind-focused/FP04/R63/challenging/work-evidence/rollup-checkpoint.sqlite` will require its own explicit SQL preservation registration before durable checkpointing. The original is present and passed exact reuse checks now; preserved SQL must not later be described as recovered original SQLite bytes.

The bounded checks do not establish complete access transcripts, hidden correction effort, historical runtime equivalence, successful business Outcomes, or broad ALPS release readiness. Those remain separate evidence and reporting questions under the fixed comparison plan.
