# Independent recovered-evidence sufficiency review

C071 and C072 have sufficiently supported complete final authored text versions for their fixed future consumers C-U141–144. The eight creator extracts and all 32 consumer evidence supplements pass the source-identity and extraction checks described below. This approves use of the recovered authored content and admission of accurately labelled evidence; it does not restore missing historical filesystem observations or grade any business application.

The coordinator reports checkpoint `fcedf4b0c327843f334ae0b8a547fa09f6cfc48e` and its 51 changed files were read-back verified. This review inspected the named recovery files in `/workspace/scratch/a75c3a6d9076/alps-v070-assessment`, compared source relays and original assignment/input evidence with the retained immutable `75c0ea9bddf379c5e1803e4c0114ba5b47c871d8` checkout, and performed independent byte/hash and text-boundary checks. No business operation, creator/consumer execution, original database access, repair, or trial-file change was performed. Only this review report was written. Restored-path helper implementation and review are outside this assignment.

## Creator evidence and sufficiency

`recovery/extracted-creators/provenance.json` has SHA-256 `dbc8daf266df30f2cdd99216491caebd0937a5209f69be3acaea1ee83b1209c2`. Its eight listed files exactly cover the extracted creator inventory. For every file, I independently matched the named original path to the corresponding outer fenced body in the unchanged relay, checked the recorded one-based body-start line, compared the complete extracted bytes including their present terminal newline, and verified the recorded extracted size, SHA-256 and observed mode. All checks passed. Both relay hashes match their unchanged immutable-checkout sources.

| Creator | Recovered supplied package | Content supporting completeness | Fixed consumers |
|---|---|---|---|
| control-071 | `checkout-release`: `SKILL.md`, `references/release-interface.md`, `assets/release-request.json` | The relay identifies all four authored files, including the separate public note. The note enumerates exactly these three package resources, reports their observed file list, and states no demonstration was requested or created. Both linked resources are present. | C-U141 ordinary; C-U142 challenging |
| control-072 | `checkout-service-release`: `SKILL.md`, `references/interface.md`, `assets/release-context.template.json` | The relay identifies all four authored files, including the separate public note. The note explicitly identifies a three-file package, reports its three-file check and no separate demonstration, and both linked resources are present. | C-U143 ordinary; C-U144 challenging |

The original creator assignments are byte-identical to the immutable-checkout assignments. Both match their fixed S10 schedule cells, including creator identity, repetition, requested model/effort and fresh-context designation. Each original prompt matches its recorded SHA-256. Each original input directory contains exactly the assigned `brief.md` and `release_tool.py`, and their hashes match the assignment and frozen main-case bank:

- `brief.md`: `284eab48172c8b996d5eb3218590fb453c1e24f5b532f9d4457bda99fb38485e`.
- `release_tool.py`: `939931810a40d9f813280ea46c95fec2c4a1ed72368937353bf5c899b8217577`.

Those are also the input hashes retained in both original public notes. The original prompt requires a self-contained Skill and a separate public note, with no unrequested demonstration. The observed three-resource packages and separate notes therefore cover the authored outputs identified by the retained completion evidence. No bundled executable, database, separately requested demonstration, or fourth package resource is identified as missing. This is a finding about the completeness and identity of retained authored content, not the correctness of its instructions or a promise of consumer adequacy.

The evidence is sufficient because these are the retained texts of successful final authoring operations, corroborated by the original notes' complete output inventories and the unchanged original task identities. They are not newly generated replacements chosen after consumer outcomes. The four dependent consumer contexts have never started, so consuming this recovered version does not overwrite a prior application or select a better result. Retain the existing creator completions and the fixed consumer slots.

The original authored-file byte hashes, modes, final filesystem inventories, coordinator physical-format reports, frozen package copies, freeze manifests, and lost earlier consumer preparation metadata remain unavailable. Current extraction hashes and mode `0644` are new recovery observations. The retained notes explicitly distinguish their own static checks from the official format validator; those notes cannot substitute for the missing coordinator reports. Neither the completion notification nor this review proves the absent historical freeze bytes. These limitations remain in the evidence and any eventual eligibility discussion.

## Minimal approved placement, validation and freezing

The following is an approved recovery procedure for the two exact reviewed text versions. It authorizes no source edit or business replay.

1. Preflight the whole two-creator/four-consumer recovery batch. Recheck the extraction provenance hashes and all eight resource hashes. Require every intended new destination to be absent, every existing parent to be an ordinary directory, and no symlink or partial output to be replaced. At this review, the two authored-output trees and notes, both canonical freezes/manifests and historical format-report paths, and consumer/setup/assignment/snapshot destinations C-U141–144 were absent. Existing creator prompts, assignments and inputs must remain unchanged. Recheck these conditions immediately before writes because other coordinator work can continue.

2. Record a distinct recovery observation, for example `recovery/creator-rematerialization-001.json`, identifying this review, the extraction provenance hash, both unchanged relay hashes, original missing paths, proposed destinations, and the explicit historical gaps. Copy the six package texts and two separate notes byte-for-byte with exclusive creation into their missing `trials/control-071/...` and `trials/control-072/...` locations. Use the reviewed readable regular-file mode `0644` as a declared new placement mode; do not call it the original mode. Verify copied hashes against the extraction manifest. Add no recovery text inside either Skill or its preserved public note. Do not use overwrite-oriented restoration or regenerate the files from their business requirements.

3. Run fresh physical-format observations using the same pinned `agentskills/agentskills@f130f348f502d9804278a617f86929846896d2e9` validator and environment used by the approved `validate_control.py`. Preserve the actual command, validator identity, target resource hashes, current observation time, exit code and exact stdout/stderr in new, exclusive reports such as `validation/control-format-recovery-001/control-071.json` and `control-072.json`. Do not populate the absent historical `validation/control-format/control-071.json` or `control-072.json` with invented prior results. Directly invoking the same pinned validator and capturing these separately labelled results is the minimal permitted adaptation of the approved mechanical check; no new validator or revised criteria is authorized.

   The existing command shape is `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=/workspace/scratch/a75c3a6d9076/validation-env/lib/python3.12/site-packages python3 /workspace/scratch/a75c3a6d9076/validation-env/bin/skills-ref validate <exact recovered Skill directory>`. Use each reconstructed directory, or the byte-identical extracted directory while recording that exact target. Verify the pinned runtime is available; do not silently substitute another validator. A real failure is retained and investigated as a new mechanical observation, not repaired away or replaced with the lost reported pass.

4. After passing fresh mechanical checks, use the unchanged approved `prepare_control_consumers.py` at SHA-256 `3eb8b617b9bd7264a25d871d675ff02c5c9e55b8df3d659c22c815cdf18af581`, with its approved dependencies, for the explicit completed IDs:

   ```sh
   python3 -B prepare_control_consumers.py control-071 control-072
   ```

   This rebuilds preparation for C-U141–144 from the fixed case bank and creates fresh canonical freeze copies, manifests and assignments. It does not rerun either creator or start a consumer. Preserve the helper's refusal of partial/existing destinations and all original schedule, prompt, input, byte and mode checks. If an implicated helper dependency has changed, its separate required review must pass before invoking that changed implementation.

5. Before consumer execution, write an exclusive, explicitly labelled companion record such as `recovery/creator-recovery-freezes-001.json`. Bind this review and the rematerialization/format observations to the exact hashes of both newly created canonical freeze manifests, all frozen resource hashes/modes and the four newly created consumer assignments. State that these are **new recovery freezes and reconstructed preparation**, that `source_file_modes` records the newly placed source modes, and that the old freeze and preparation bytes remain missing. A companion record permits the unchanged approved helper to retain its normal canonical paths without presenting its new output as a recovered historical freeze. Preserve the reported earlier preparation as a historical observation rather than claiming the original packets had never existed.

6. Record current preparation readiness without fabricating historical timestamps, checkpoint all recovered content, new labelled observations/freezes, fixed packets and limitations, and verify the save before the four first consumer starts. Use their fixed fresh reference configuration and normal restricted consumer prompts. Recovery, format checking and preparation add no task trial, retry or reserve start. No fresh creator, source repair, candidate development round or outcome-based replacement is permitted by this procedure.

These operations may be performed only as the specified recovery observations. Ordinary business grading and provisional example eligibility remain separate, with the missing historical evidence visible. This review does not waive any requirement for working bundled processing, independent use or later example checks.

## Consumer extraction and SQL provenance

`recovery/consumer-evidence/provenance.json` has SHA-256 `4a24e1cb17327ea2b05e11b9c8cabcd2324e6b0ec6f9a8f66bbadadd322bf35b`. Its listed inventory exactly matches the 32 files present: eight answers, eight public notes, seven checkpoint renderings and nine matched-SQL supplements. No additional or missing file was found in that inventory.

For each of the 23 extracted text files, the recorded inclusive source lines are exactly the content between the corresponding relay's display fences, with neither fence included. Extracted bytes and recorded SHA-256 match; every source relay is byte-identical to the immutable `75c0ea9` source, and its recorded SHA-256 and Git blob identity match independently calculated identities. The seven checkpoint renderings parse as JSON. No extraction discrepancy or omitted final note addition was found within the reviewed relay bodies. These checks verify accurate extraction of the relays, not the truth or completeness of every historical agent report.

For all nine native-SQL supplements, bytes exactly equal the named committed source and match its Git blob and recorded SHA-256. Each consumer's fixed S06 variant matches the ordinary/challenging digest in the historical capture observation:

| Reported captured endpoints | Committed byte source | SHA-256 |
|---|---|---|
| C-U085, 087, 089, 091, 093 | `state-snapshots/C-U073/final.sql`, blob `c78e22eb4a0b747e66ced5f0b91872d751dd4c24` | `0c59cdc1f01c69cb8dc8899d6565287e22fc0469988613be1a04c379594ca580` |
| C-U084, 086, 088, 090 | `state-snapshots/C-U074/final.sql`, blob `19f0c73867febe10a8e69e4a7818f1ddd0ebd7c0` | `071ead7c2855dc57b35546ff0515c79039899eae12b921f60584232f858cb496` |

The historical binding source is the `Uncommitted state evidence to recover` section of `RECOVERY-ENVIRONMENT-OFFLINE.md` at commit `75c0ea9bddf379c5e1803e4c0114ba5b47c871d8`, blob `2f715090c38b97140716ef3167d7c5f35b6e2660`, SHA-256 `c5d3d84c6a3775b875a5051415cf2344a14118f7c426981b4f65469a3ef6200b`. The current document has an added restoration-status prefix; the historical section is preserved unchanged. The supplements accurately cite the historical commit, so that legitimate later prefix does not break their provenance.

The two distinct SQL contents already passed in-memory restoration, integrity and exact-dump checks in the prior independent environment-restoration review. The current supplements are byte-identical to those checked sources; no redundant database execution was needed here. Matching a retained captured digest supports these exact logical-state bytes. It does not supply the absent original per-trial final metadata, original SQLite bytes, runtime file identity, complete call history or a new final-state observation. The present filenames and scope labels preserve this distinction correctly. Do not install these files as fabricated `state-snapshots/C-Uxxx/final.sql`/`final.json` captures merely to satisfy the normal grading helper.

## Specific remaining gaps and allowed evidence use

C-U084/C-U085 original answer/note/checkpoint corroboration remains covered by the prior review. The new extraction inventory appropriately avoids replacing those originals. C-U086–093 public texts and their available checkpoint renderings may be included in independently reviewed grading packets under their explicit recovered-evidence labels and source provenance. Preserve missing post-execution resource inventories: a restored initial resource copy is not an observed final resource inventory. Normal blind packaging must not be made to pass by manufacturing absent final files, state metadata or unchanged-resource claims.

C-U091's checkpoint is the retained pretty-printer rendering, not recovered compact bytes. C-U089's original checkpoint relay explicitly makes no trailing-newline claim; its current rendering includes the display newline and must remain a rendering. These are representation limits, not extraction failures.

An additional exact byte recovery is available for C-U092's **workflow request checkpoint**, independently of its missing API endpoint. The present `checkpoint-rendering.json` is the exact 1,455-byte relay body and has SHA-256 `b0423a23f9d12352c45e90dc2609f2125224c0cde340d5ac3f58174209d03a4e`. Removing exactly its one terminal display LF gives 1,454 bytes and SHA-256 `cf3b8effece5f3c1c9be5e405b5240a66d68d83c984a9795304481974fc486ee`, matching both historical size and digest recorded by the original successful listing/hash command in relay lines 124–126. A new separately named `request-hash-matched.json` supplement may preserve those verified bytes, with the exact transformation and historical source recorded. Keep the existing rendering and relay unchanged. This is content-addressed evidence recovery, not a workflow replay, a lock-file recovery, or a final API capture.

C-U092 final API SQL and its historical final digest remain absent. C-U093's workflow SQLite bytes/native SQL remain absent despite preserved historical hashes; its matched API-state supplement is a different database. Missing locks, journals and uncaptured output remain unavailable. Do not initialize, page, grant quota, rerun a workflow or recreate an empty sidecar to make those observations appear complete.

Completed original applications remain completed. Business reviewers may use the recovered evidence and public observations to support the claims they actually establish; unsupported required observations remain unconfirmed, with the original denominator and missingness rules retained. This review neither awards grades nor declares all affected applications invalid. No completed application may be rerun to obtain stronger evidence or a better result. Independent infrastructure adjudication and at most one distinct first-valid retry remain required only where the original protocol permits a retry; the four remaining reserve starts and prohibition on a fourth ordinary improvement round are unchanged.
