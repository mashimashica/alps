# Independent review of C-control preparation

Preparation has one blocking bookkeeping defect: the reciprocal guards do not consistently recognize interrupted preparation before its ledger exists. Correct that defect before actual preparation. The specified allocation, C authoring conditions, original public-input provenance, metadata placement and limits on interpretation otherwise satisfy this preparation-only review. No creator or consumer task was executed, and successful synthetic packaging is not evidence of generated-Skill quality.

I read `protocol.md`, `gates.md`, `audits/assessment-dependency-review.md`, `control-stage.md`, both preparation scripts and `main-case-hashes.json`. I did not inspect generated outputs, grades, B sources or the product worktree. No applicable ancestor `AGENTS.md` was present. I checked the public bank by file names and SHA-256 without examining its business content; reading briefs was unnecessary because prompt equivalence could be established directly. All 36 files exactly matched the manifest, with no additional files or symlinks. Both specified common resource paths exist. This verifies the current public bank against the supplied manifest, not the manifest's historical origin or the complete authoring aid's independent freeze history.

The control loops allocate precisely S01, S03, S05, S06, S09 and S10 across the three prescribed models, low/high effort and two repetitions: 72 distinct creators. There is no result-selected family or configuration, substituted main arm, or additional base allocation. The stage document retains two independent gpt-5.6-sol/high consumers per artifact, totaling the original 144 consumer slots. The helper packages only creators, so consumer assignments, independent cases and actual model availability remain later checks. Its deterministic order and `control-001`–`control-072` IDs are fixed before execution; two consumer observations remain clustered within their generated artifact.

I compared the scripts' C prompt expressions after normalizing task paths. Their aid, format, requested outputs, demonstration limits, available verification tools, resource restrictions and public execution-note instructions match exactly apart from two declared additions: use the task directory for shell commands and authored/disposable state, and prohibit workspace/parent enumeration. These implement the protocol's procedural isolation. The same frozen skill-creator remains optional, common format information is supplied, and ALPS authoring guidance is absent. Future A/B preparation should preserve equivalent operational restrictions when it becomes authorized. Nothing in these additions changes the brief's responsibilities, forces a demonstration or requires ALPS headings.

The helper verifies the bank before writing, hashes copied inputs against the original manifest, and records prompt and input hashes in `control-assignments/`, outside each creator task. The synthetic positive run confirmed 72 unique original cells, C/control labels, expected IDs, matching schedule digest, read-only schedule/digest, correct input/prompt hashes, and no assignment metadata within task directories. Task trees contained only public inputs, prompts and empty deliverable destinations. No generated artifact or consumer input was created. Exclusive creation and the separate status ledger preserve the intended distinction between allocation and execution; filesystem permissions and supplied-path instructions do not enforce agent isolation or guarantee durable provenance. The coordinator's review and verified checkpoint remain required before the first creator.

Disposable checks used copied helpers, twelve trivial public briefs and synthetic common-resource/candidate placeholders, wholly outside the assessment. Missing `--candidate` failed with exit 2; a missing selection document failed with exit 1. Repeating completed control preparation failed with exit 1, and main preparation after a completed control ledger also failed with exit 1. The existing candidate-name, documented-selection and selected-source prerequisites remain in main; qualification itself is still a substantive coordinator decision, not inferred from a file's existence. The new helper neither invokes main nor supplies an alternative A-only or candidate-free main mode.

Two interruption checks failed the required preservation safeguard. First, after successful synthetic control preparation, removing only the final ledger to represent interruption immediately before its creation left schedule, assignments and tasks intact; main nevertheless exited 0 and assigned all 72 C cells again under main IDs. Second, precreating synthetic `trials/main-004` caused main to stop at that directory after assigning `main-003` to C but before writing either ledger or schedule. Control then exited 0 despite that existing C assignment. Cover partial schedules, digests, assignment directories and relevant trial paths reciprocally, including dangling symlinks, without deleting or silently reconciling evidence. Retest these concrete refusal paths after correction; no permanent mirror-implementation tests are needed.

The source versions checked were `prepare_control.py` SHA-256 `6987f5b51e8d152301a61850d9ac476351321870779409550dd72c9be83164d6` and `prepare_main.py` SHA-256 `d6fb9d70a61f2dd683aef3773e80012d26fdc489ad426fc22ff82249957c2a67`. Disposable trees were removed. Actual preparation, checkpoint identity, creator access reports, consumer independence, oracle quality and grading were not performed or certified here. The documents preserve fixed meanings and criteria, the qualified-B main block, downstream dependencies, retry accounting and the prohibition on treating descriptive C evidence as ALPS benefit, convergence or release readiness. Resolving the packaging defect does not resolve those assessment dependencies.

**Follow-up after correction: the execution blocker is resolved for the versions below.** The initial finding above remains the historical review. I reread both helpers and `control-stage.md`. Disposable control preparation exited 0 with the unchanged 72-cell allocation. Main now refused control's missing-ledger residual state; control refused the reconstructed pre-ledger main state, including its assigned C cell. Both exited 1 and preserved complete synthetic tree snapshots. Six dangling-symlink cases—opposite ledger, opposite trial entry and `trials` root in each helper—also exited 1 without writes. Missing/invalid candidate, missing selection and missing selected-source checks retained expected failures (2/1/1/1). No actual preparation or trial access occurred; disposable trees were removed. Coordinator preparation review/checkpoint and all substantive dependencies remain.

| Reviewed file | SHA-256 |
| --- | --- |
| `prepare_control.py` | `f37a0cd6a3b1963b485028117a26e06f5c03217f568b1bea467b16f0a6d6db87` |
| `prepare_main.py` | `0053bd764d477ae915e70df40b1876de5943b275674c4d017331c9f43bca6e6f` |
| `control-stage.md` | `eb28608c268b306cf42f7aab32361b51a94259275ab65849cefd91e10d058bff` |
