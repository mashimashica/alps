interface:
  display_name: Checkout Service Release
  short_description: Qualify, approve, promote, and verify an exact checkout release
  default_prompt: Release the exact requested checkout-service candidate using the supplied local release command, then verify production health and checkout and report evidence, blockers, and uncertainty.
