interface:
  display_name: Checkout Service Release
  short_description: Qualify, promote, and verify a checkout-service release
  default_prompt: Release the exact authorized checkout-service candidate using the supplied local simulation, then verify health and checkout.
